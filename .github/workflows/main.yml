name: Build Android APK
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          # یہاں ہم وہ تمام فائلیں ڈال رہے ہیں جو آخری مرحلے (Packaging) کے لیے ضروری ہیں
          sudo apt-get install -y build-essential libffi-dev libssl-dev python3-pip \
          zip unzip openjdk-17-jdk libncurses5 libncursesw5 zlib1g-dev \
          libjpeg-dev libfreetype6-dev pkg-config ant
          
          pip install --upgrade pip
          pip install "cython<3.0" buildozer
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Build with Buildozer
        run: |
          # اینڈرائیڈ سسٹم کا راستہ سیٹ کرنا تاکہ ریڈ وارننگ نہ آئے
          export APP_ANDROID_SDK=$ANDROID_HOME
          # 'yes |' خود بخود لائسنس قبول کرے گا
          yes | buildozer android debug --verbose

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VIP-Vault-APK
          path: bin/*.apk
